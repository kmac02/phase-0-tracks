// Find the longest phrase in an array. So if we gave your function the array of ["long phrase","longest phrase","longer phrase"], it would return "longest phrase".

// Create array of phrases.
testArray(["long phrase","longest phrase","longer phrase"])
// find the length of each string in the array

// compare the lengths of each string
// return the longest string

